version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: agent932-davinci-studio-server_studio_1
      APP_PORT: 8543

  studio:
    image: allebb/studio-server:latest@sha256:b96c9cc9f665a7075b962f88b83309a95598218d48a78f8eb88a7065900db0ec
    restart: unless-stopped
    ports:
      - "5432:5432"
      - "50059:50059"
    environment:
      # umbrelOS has no settings form for community apps, so set your timezone
      # here in ~/umbrel/app-data/agent932-davinci-studio-server/docker-compose.yml
      # and restart the app, e.g. TZ=America/Edmonton
      - TZ=${TZ:-Europe/London}
    volumes:
      - ${APP_DATA_DIR}/data/hooks:/var/studio-server/hooks
      - ${APP_DATA_DIR}/data/database:/var/studio-server/database
      - ${APP_DATA_DIR}/data/backups:/var/studio-server/backups
      - ${APP_DATA_DIR}/data/jobs:/etc/cron.d